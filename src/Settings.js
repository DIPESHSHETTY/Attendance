import React from 'react';
import './Settings.css';

const Settings = () => {
  return (
    <div className="settings">
      <button className="dark-mode-toggle">Logout</button>
    </div>
  );
};

export default Settings;
